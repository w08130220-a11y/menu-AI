# Database（開發環境使用 SQLite，正式環境可改用 PostgreSQL）
DATABASE_URL="file:./dev.db"

# Session 簽章密鑰（openssl rand -base64 32）
AUTH_SECRET="change-me-in-production"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
